<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.epam.rd.tasks.zoo.repository.animals.crustacean.highcancer.crab.CrabRepository">

    <resultMap id="crabResultMap" type="com.epam.rd.tasks.zoo.animal.crustacean.highercancers.Crab">
        <result column="name" property="name"/>
        <result column="describe" property="describe"/>
        <result column="age" property="age"/>
        <result column="isDeleted" property="isDeleted"/>
        <result column="seashell" property="seashell"/>
        <result column="taste" property="taste"/>
    </resultMap>

    <insert id="create" >
        BEGIN work;
        LOCK TABLE animal;
        INSERT INTO animal (name, describe, age, id_animaltype, isDeleted, createDate)
        VALUES (#{animal.name}, #{animal.describe}, #{animal.age}, (SELECT id FROM animalType WHERE animalType.animalType = #{animal.typeOfAnimal}),
        false, CURRENT_DATE);

        INSERT INTO crustacean (id, seashell) VALUES ((select MAX(id) FROM animal), #{seashell});
        INSERT INTO bullfinch(id, taste) VALUES ((select MAX(id) FROM animal), #{taste});
        COMMIT work;
    </insert>

    <select id="findById" resultMap="crabResultMap">
        SELECT * FROM ANIMAL
        INNER JOIN crustacean ON crustacean.id = ANIMAL.id
        INNER JOIN crab ON crab.id = ANIMAL.id
        WHERE ANIMAL.id = #{id}
    </select>

    <update id = "update">
        UPDATE crab SET taste = #{animal.taste} WHERE ID = #{animal.id};
        UPDATE crustacean SET seashell = #{animal.seashell} WHERE ID = #{animal.id};
        UPDATE animal SET name = #{animal.name} ,describe = #{animal.describe}, age = #{animal.age} WHERE ID = #{animal.id} AND isdeleted = #{animal.isDeleted};
    </update>

    <update id = "setDeleteStatus">
        UPDATE animal SET isdeleted = #{status} WHERE ID = #{id};
    </update>

    <delete id="hardDelete">
        DELETE FROM crab WHERE id = #{id};
        DELETE FROM crustacean WHERE id = #{id};
        DELETE FROM animal WHERE id = #{id};
    </delete>
</mapper>