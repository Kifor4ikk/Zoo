<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.epam.rd.tasks.zoo.repository.animals.bird.BullfinchRepository">

    <resultMap id="bullfinchResultMap" type="com.epam.rd.tasks.zoo.animal.bird.finche.Bullfinch">
        <result column="name" property="name"/>
        <result column="describe" property="describe"/>
        <result column="age" property="age"/>
        <result column="isDeleted" property="isDeleted"/>
        <result column="wings" property="wings"/>
        <result column="color" property="color"/>
    </resultMap>

    <insert id="create" >
        BEGIN work;
        LOCK TABLE animal;
        INSERT INTO animal (name, describe, age, id_animaltype, isDeleted, createDate)
        VALUES (#{animal.name}, #{animal.describe}, #{animal.age}, (SELECT id FROM animalType WHERE animalType.animalType = #{animal.typeOfAnimal}),
        false, CURRENT_DATE);

        INSERT INTO bird (id, wings) VALUES ((select MAX(id) from animal), #{animal.wings});
        INSERT INTO bullFinch(id,color) VALUES ((select MAX(id) from animal), #{animal.color});
        COMMIT work;
    </insert>

    <select id="findById" resultMap="bullfinchResultMap">
        SELECT * FROM ANIMAL
        INNER JOIN bird ON bird.id = ANIMAL.id
        INNER JOIN bullfinch ON bullfinch.id = bird.id
        WHERE animal.id = #{id}
    </select>

    <update id = "update">
        UPDATE bullfinch SET color = #{animal.color} WHERE ID = #{animal.id};
        UPDATE bird SET wings = #{animal.wings} WHERE ID = #{animal.id};
        UPDATE animal SET name = #{animal.name} ,describe = #{animal.describe}, age = #{animal.age} WHERE ID = #{animal.id} AND isdeleted = #{animal.isDeleted};
    </update>

    <update id = "setDeleteStatus">
        UPDATE animal SET isdeleted = #{status} WHERE ID = #{id};
    </update>

    <delete id="hardDelete">
        DELETE FROM bullfinch WHERE id = #{id};
        DELETE FROM bird WHERE id = #{id};
        DELETE FROM animal WHERE id = #{id};
    </delete>
</mapper>